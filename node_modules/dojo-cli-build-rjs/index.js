module.exports = {
	description: 'this is my r.js command',
	options: [
		'name'
	],
	register() {
		console.log('register');
	},
	run(argv) {
		console.log(`ran ${argv}`);
		new Promise((resolve) => setTimeout(resolve, 3000)).then(() => console.log('done'));
	}
}
