module.exports = {
	name: 'webpack',
	description: 'this is my webpack command',
	options: [
		'name'
	],
	register(yargs) {
		return yargs.option('url', {
			alias: 'u',
			default: 'http://yargs.js.org/'
		})
	},
	run(argv) {
		console.log(`ran ${argv}`);
	}
}
